extends Control

class_name AlchemyTable


# Called when the node enters the scene tree for the first time.
func _ready():
	# var alchemist_table_element = AlchemyTableElementScene.instantiate()
	# add_child(alchemist_table_element)
	# alchemist_table_element = AlchemyTableElement.new()
	# alchemist_table_element.initialize_element('Fire')
	# add_child(alchemist_table_element)
	pass


func _physics_process(delta):
	pass
	# print(get_children())
	# var start = rect_a.global_position + rect_a.size / 2
	# var end = rect_b.global_position + rect_b.size / 2
	# var center = (start + end) / 2
	# var distance = start.distance_to(end)
	# var angle = start.angle_to_point(end)
	#
	# line.global_position = center - Vector2(distance / 2, line.size.y / 2)
	# line.size.x = distance
	# line.rotation = angle
