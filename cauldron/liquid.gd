class_name Liquid

var aspect: AspectElement

var current_pipe: Pipe

var direction: Pipe.Puts


func _init(_aspect: AspectElement, start_pipe: Pipe, start_direction: Pipe.Puts) -> void:
	aspect = aspect
	current_pipe = start_pipe
	direction = start_direction
